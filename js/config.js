/* ============================================================
   全站設定
   ------------------------------------------------------------
   這支要第一個載入，其他檔案都掛在 CH 這個名字底下。
   ============================================================ */

var CH = { DATA: {} };   // DATA 由 data/ 裡的四個檔案填進來

CH.CONFIG = {

  /* ---------- 圖片資料夾 ---------- */
  images: {
    members: 'images/members/',   // 角色立繪  1.員工1.png
    cards  : 'images/cards/'      // 牌面      愚者.png
  },

  /* 找圖時依序嘗試的副檔名（資料檔只寫名字、不寫副檔名）。
     用不到的可以刪掉，載入時就少幾次白跑的請求。 */
  imageExtensions: ['jpg', 'png', 'jpeg', 'webp', 'gif', 'avif'],

  /* ---------- 店員輪播 ---------- */
  roster: {
    duration : 5000,   // 自動換人的間隔（毫秒）
    threshold: 60,     // 滑動超過幾 px 才算換人
    outMs    : 230,    // 舊的人滑出去
    inMs     : 420,    // 新的人滑進來
    offset   : 52      // 進出場的位移距離
  },

  /* ---------- 抽牌 ---------- */
  tarot: {
    // 'auto' = 牌義有寫「逆位關鍵字／逆位解析」才會出現逆位
    // true   = 一律隨機正逆位（沒寫逆位就沿用正位文字）
    // false  = 永遠正位
    reversed: 'auto',
    flipBackMs: 640   // 重抽時先蓋回去的時間
  }
};
